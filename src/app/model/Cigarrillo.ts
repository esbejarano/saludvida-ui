export class Cigarillo {
    public fuma: number;
    public cantidad: number;
    public observaciones: string;

    constructor( fuma: number, cantidad: number, observaciones: string ) {
        this.fuma = fuma;
        this.cantidad = cantidad;
        this.observaciones = observaciones;
    }
}
